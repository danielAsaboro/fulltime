'use strict'

const ROOM_PROTOCOL_VERSION = 1
const ROOM_PROTOCOL_NAME = 'fulltime/room/1'
const PRESENCE_PROTOCOL_NAME = 'fulltime/room-presence/1'
const ROOM_DATA_EPOCH = 1

const MAX_ROOM_MEMBERS = 256
const MAX_ROOM_MODERATORS = 32
const MAX_ROOM_NAME_LENGTH = 48
const MAX_DISPLAY_NAME_LENGTH = 48
const MAX_MESSAGE_LENGTH = 1000
const MAX_POLL_QUESTION_LENGTH = 160
const MAX_POLL_OPTION_LENGTH = 80
const MAX_POLL_OPTIONS = 5
const MAX_HISTORY_ITEMS = 200
const MAX_REPLY_ITEMS = 200
const MAX_PAGE_ITEMS = 100
const MAX_PAGE_CURSOR_LENGTH = 1024
const MAX_INVITE_CODE_LENGTH = 4096
const MAX_INVITE_PREVIEW_BYTES = 2048
const MAX_OPERATION_BYTES = 24 * 1024
const MAX_ROOM_IPC_BYTES = 2 * 1024 * 1024
const PAIRING_TIMEOUT_MS = 45_000
// Hyperswarm only re-queries a topic on a long background cadence. A local
// room write is a meaningful opportunity to reannounce/requery after a peer
// has restarted, without turning idle rooms into a polling loop.
const ROOM_DISCOVERY_REFRESH_MS = 5_000

module.exports = {
  MAX_DISPLAY_NAME_LENGTH,
  MAX_HISTORY_ITEMS,
  MAX_INVITE_CODE_LENGTH,
  MAX_INVITE_PREVIEW_BYTES,
  MAX_MESSAGE_LENGTH,
  MAX_OPERATION_BYTES,
  MAX_PAGE_CURSOR_LENGTH,
  MAX_PAGE_ITEMS,
  MAX_POLL_OPTIONS,
  MAX_POLL_OPTION_LENGTH,
  MAX_POLL_QUESTION_LENGTH,
  MAX_REPLY_ITEMS,
  MAX_ROOM_IPC_BYTES,
  MAX_ROOM_MEMBERS,
  MAX_ROOM_MODERATORS,
  MAX_ROOM_NAME_LENGTH,
  PAIRING_TIMEOUT_MS,
  PRESENCE_PROTOCOL_NAME,
  ROOM_DISCOVERY_REFRESH_MS,
  ROOM_DATA_EPOCH,
  ROOM_PROTOCOL_NAME,
  ROOM_PROTOCOL_VERSION
}
