'use strict'

// bare-pack traverses both sides of runtime feature checks. Resolve the Node
// builtin spelling to its Bare equivalent in the mobile worker graph.
module.exports = {
  path: 'bare-path'
}
